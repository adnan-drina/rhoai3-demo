- op: replace
  path: /spec/channel
  # Verified 2026-06-11 on OCP 4.20.24: gitops-1.20 is the highest stable channel.
  # oc get packagemanifest openshift-gitops-operator -o jsonpath='{.status.channels[*].name}'
  value: gitops-1.20
