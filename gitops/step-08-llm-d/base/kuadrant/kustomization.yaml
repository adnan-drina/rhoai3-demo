apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Kuadrant Instance for llm-d Gateway Integration
# Required by LLMInferenceService for AuthPolicy validation
#
# NOTE: The RHCL Operator is installed in Step 01
#       This creates the Kuadrant instance required by llm-d

resources:
  - kuadrant.yaml

