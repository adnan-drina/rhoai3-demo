# Kuadrant Instance for llm-d Gateway Integration
# The llm-d controller requires a Kuadrant instance to be present
# for Gateway API integration and AuthPolicy validation
#
# Ref: https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.0/html-single/deploying_models/index
apiVersion: kuadrant.io/v1beta1
kind: Kuadrant
metadata:
  name: kuadrant
  namespace: private-ai
  labels:
    app.kubernetes.io/name: kuadrant
    app.kubernetes.io/component: gateway-integration
    app.kubernetes.io/part-of: llm-serving
    demo.rhoai.io/step: "08"
  annotations:
    argocd.argoproj.io/sync-wave: "0"
    openshift.io/display-name: "Kuadrant Gateway Integration"
    openshift.io/description: "Provides AuthPolicy support for llm-d distributed inference"
spec: {}

