apiVersion: datasciencecluster.opendatahub.io/v2
kind: DataScienceCluster
metadata:
  name: default-dsc
spec:
  components:
    # Stage 210 enables the standard KServe-based model serving platform.
    # RHOAI 3.4 schema verification on cluster-klvxt:
    # oc explain datasciencecluster.spec.components.kserve --api-version=datasciencecluster.opendatahub.io/v2
    # Only RawDeployment mode is supported by the active component.
    kserve:
      managementState: Managed
