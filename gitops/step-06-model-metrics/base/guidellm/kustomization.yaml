apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Manual trigger: oc create job --from=cronjob/guidellm-daily bench-$(date +%H%M) -n private-ai
# Ref: https://github.com/vllm-project/guidellm

resources:
  - rbac.yaml
  - cronjob.yaml
  - pipeline-pvc.yaml

