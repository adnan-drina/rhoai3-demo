# =============================================================================
# LlamaStackDistribution - RAG Backend (RHOAI 3.3 Production Pattern)
# =============================================================================
# Aligned with RHOAI 3.3 production recommendations:
#   - PostgreSQL for metadata persistence (not SQLite)
#   - Secret-based credentials (not hardcoded env vars)
#   - MILVUS_ENDPOINT with tcp:// protocol (gRPC port 19530)
#   - Remote Milvus for vector storage
#
# Ref: https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.3/html/working_with_llama_stack/
# =============================================================================
apiVersion: llamastack.io/v1alpha1
kind: LlamaStackDistribution
metadata:
  name: lsd-rag
  namespace: private-ai
  labels:
    opendatahub.io/dashboard: "true"
    app.kubernetes.io/name: llamastack-rag
    app.kubernetes.io/part-of: rag-pipeline
    app.kubernetes.io/component: rag-backend
  annotations:
    argocd.argoproj.io/sync-wave: "4"
    argocd.argoproj.io/sync-options: SkipDryRunOnMissingResource=true
    openshift.io/display-name: lsd-rag
spec:
  replicas: 1
  server:
    distribution:
      name: rh-dev
    containerSpec:
      name: llama-stack
      port: 8321
      env:
        # vLLM credentials from Secret (RHOAI 3.3 recommended)
        - name: VLLM_API_URL
          valueFrom:
            secretKeyRef:
              name: llamastack-vllm-secret
              key: VLLM_URL
        - name: VLLM_URL
          valueFrom:
            secretKeyRef:
              name: llamastack-vllm-secret
              key: VLLM_URL
        - name: VLLM_API_TOKEN
          valueFrom:
            secretKeyRef:
              name: llamastack-vllm-secret
              key: VLLM_API_TOKEN
        - name: VLLM_TLS_VERIFY
          value: "false"
        - name: VLLM_MAX_TOKENS
          value: "4096"
        # PostgreSQL metadata store (RHOAI 3.3 production requirement)
        - name: POSTGRES_HOST
          value: llamastack-postgres.private-ai.svc.cluster.local
        - name: POSTGRES_PORT
          value: "5432"
        - name: POSTGRES_DB
          value: llamastack
        - name: POSTGRES_USER
          value: llamastack
        - name: POSTGRES_PASSWORD
          valueFrom:
            secretKeyRef:
              name: llamastack-postgres-secret
              key: POSTGRES_PASSWORD
        # Remote Milvus from Secret (RHOAI 3.3 recommended)
        - name: MILVUS_ENDPOINT
          valueFrom:
            secretKeyRef:
              name: llamastack-milvus-secret
              key: MILVUS_ENDPOINT
        - name: MILVUS_TOKEN
          valueFrom:
            secretKeyRef:
              name: llamastack-milvus-secret
              key: MILVUS_TOKEN
        # Guardrails (activates when step-10 is deployed)
        - name: FMS_ORCHESTRATOR_URL
          value: http://guardrails-orchestrator.private-ai.svc:8080
      resources:
        requests:
          cpu: 250m
          memory: 500Mi
        limits:
          cpu: "2"
          memory: 12Gi
    userConfig:
      configMapName: llama-stack-rag-config
---
# =============================================================================
# LlamaStack RAG Configuration — v0.4.2.1+rhai0 Schema
# =============================================================================
apiVersion: v1
kind: ConfigMap
metadata:
  name: llama-stack-rag-config
  namespace: private-ai
  labels:
    llamastack.io/distribution: lsd-rag
    opendatahub.io/dashboard: "true"
  annotations:
    argocd.argoproj.io/sync-wave: "4"
    argocd.argoproj.io/sync-options: Replace=true
data:
  config.yaml: |
    version: 2
    image_name: custom
    apis:
    - agents
    - datasetio
    - files
    - inference
    - safety
    - scoring
    - tool_runtime
    - vector_io
    providers:
      inference:
      - provider_id: sentence-transformers
        provider_type: inline::sentence-transformers
        config: {}
      - provider_id: vllm-granite-agent
        provider_type: remote::vllm
        config:
          base_url: http://granite-8b-agent-predictor.private-ai.svc.cluster.local:8080/v1
          max_tokens: 4096
          tls_verify: false
      vector_io:
      - provider_id: milvus-shared
        provider_type: remote::milvus
        config:
          uri: http://milvus-standalone.private-ai.svc.cluster.local:19530
          token: ""
          persistence:
            namespace: milvus_vectors
            backend: kv_default
      agents:
      - provider_id: meta-reference
        provider_type: inline::meta-reference
        config:
          persistence:
            agent_state:
              namespace: agents
              backend: kv_default
            responses:
              table_name: agent_responses
              backend: sql_default
      eval: []
      files:
      - provider_id: meta-reference-files
        provider_type: inline::localfs
        config:
          metadata_store:
            table_name: files_metadata
            backend: sql_default
          storage_dir: /opt/app-root/src/.llama/distributions/rh/files
      datasetio:
      - provider_id: huggingface
        provider_type: remote::huggingface
        config:
          kvstore:
            namespace: huggingface_datasetio
            backend: kv_default
      scoring:
      - provider_id: basic
        provider_type: inline::basic
        config: {}
      - provider_id: llm-as-judge
        provider_type: inline::llm-as-judge
        config: {}
      tool_runtime:
      - provider_id: rag-runtime
        provider_type: inline::rag-runtime
        config: {}
      - provider_id: model-context-protocol
        provider_type: remote::model-context-protocol
        config: {}
    registered_resources:
      models:
      - provider_id: sentence-transformers
        model_id: granite-embedding-125m
        provider_model_id: ibm-granite/granite-embedding-125m-english
        model_type: embedding
        metadata:
          embedding_dimension: 768
      - provider_id: vllm-granite-agent
        model_id: granite-8b-agent
        model_type: llm
        metadata: {}
      shields: []
      vector_stores: []
      datasets: []
      scoring_fns: []
      benchmarks: []
      tool_groups:
      - toolgroup_id: builtin::rag
        provider_id: rag-runtime
      - toolgroup_id: mcp::database
        provider_id: model-context-protocol
        mcp_endpoint:
          uri: "http://database-mcp.private-ai.svc:8080/sse"
      - toolgroup_id: mcp::openshift
        provider_id: model-context-protocol
        mcp_endpoint:
          uri: "http://openshift-mcp.private-ai.svc:8000/sse"
      - toolgroup_id: mcp::slack
        provider_id: model-context-protocol
        mcp_endpoint:
          uri: "http://slack-mcp.private-ai.svc:8080/sse"
      connectors: []
    server:
      port: 8321
