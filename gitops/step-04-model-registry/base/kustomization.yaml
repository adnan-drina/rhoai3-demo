apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Ref: https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.3/html-single/managing_model_registries/index
# Seed job uses internal service (:8080) to bypass OAuth for automation.

resources:
  - database/
  - registry/
  - rbac/
  - seed-job.yaml
