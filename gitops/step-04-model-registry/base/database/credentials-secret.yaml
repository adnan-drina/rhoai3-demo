# ⚠️ DEMO VALUES ONLY - Replace for production use
# Generate new passwords: openssl rand -base64 24
# For production, use:
#   - External Secrets Operator
#   - Sealed Secrets
#   - oc create secret generic with values from secure storage
apiVersion: v1
kind: Secret
metadata:
  name: model-registry-db-creds
  namespace: rhoai-model-registries
  annotations:
    argocd.argoproj.io/sync-wave: "1"
type: Opaque
stringData:
  database-name: model_registry
  database-user: mlmd
  database-password: mlmd-secret-123
  database-root-password: root-secret-456

