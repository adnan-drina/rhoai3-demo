apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Model Registry Instance
# Deployed in rhoai-model-registries namespace (operator-managed)
#
# Services:
#   - private-ai-registry:8443      - OAuth-protected (Dashboard/UI)
#   - private-ai-registry-internal:8080 - Internal API (automation jobs)

resources:
  - model-registry.yaml
  - internal-service.yaml
  - internal-networkpolicy.yaml
  - catalog-sources.yaml
