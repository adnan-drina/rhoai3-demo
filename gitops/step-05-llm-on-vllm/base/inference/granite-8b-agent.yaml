# Red Hat Validated Model (May 2025 Collection)
# Model: RedHatAI/granite-3.1-8b-instruct-FP8-dynamic
# Source: registry.redhat.io/rhelai1/modelcar-granite-3-1-8b-instruct-fp8-dynamic:1.5
# Precision: FP8 Dynamic - 8B parameters, ~8GB VRAM
# Hardware: g6.4xlarge (1x NVIDIA L4, 24GB VRAM)
# Storage: OCI ModelCar (Red Hat Model Catalog)
#   - Pulled via cluster pull secret — no S3/MinIO or HuggingFace download needed
#   - KServe modelcar sidecar mounts the OCI image as an emptyDir volume
#   - Faster cold start vs S3 (no storage-initializer download)
# Use Case: The Agentic Core - Tool-calling, Function-calling, and RAG.
# Features:
#   - Native tool-calling support (built-in chat template in tokenizer_config.json)
#   - Auto tool choice via --tool-call-parser=granite for Agent Playground
#   - 16K context window (required for MCP + RAG with 31 tool schemas)
# Performance Tuning (benchmarked with GuideLLM):
#   --kv-cache-dtype=fp8  → Doubles KV cache: 74K→155K tokens (L4 Ada native FP8)
#   --gpu-memory-util=0.92 → Safe maximum (0.95 OOMs during CUDA graph capture)
#   --max-model-len=16384  → REQUIRED for MCP demo (31 tool schemas + file_search = 12-16K input)
#   ITL: 40ms p95 (hardware-bound on L4, ~25 tok/s per request)
#   TTFT: <90ms p95 (well within 150ms SLO)
# WARNING: Do NOT reduce max-model-len below 16384.
#   The RAG chatbot with all MCP tools enabled (step-10) produces 12-16K input tokens.
#   See docs/prompt-engineering-session.md "Critical Findings #1" for details.
# Demo Role: Key model for Step 06 (Agent Playground)
# Ref: https://docs.redhat.com/en/documentation/red_hat_ai/3/html-single/validated_models/index#validated-red-hat-ai-modelcar-container-images
apiVersion: serving.kserve.io/v1beta1
kind: InferenceService
metadata:
  name: granite-8b-agent
  namespace: private-ai
  labels:
    app.kubernetes.io/name: granite-8b-agent
    app.kubernetes.io/component: inference
    app.kubernetes.io/part-of: llm-serving
    app.kubernetes.io/managed-by: argocd
    opendatahub.io/dashboard: "true"
    opendatahub.io/genai-asset: "true"  # AI Asset Endpoint for Playground
    networking.kserve.io/visibility: exposed
  annotations:
    argocd.argoproj.io/sync-wave: "10"
    opendatahub.io/hardware-profile-name: nvidia-l4-1gpu
    opendatahub.io/hardware-profile-namespace: redhat-ods-applications
    openshift.io/display-name: "Granite-3.1-8B Agent (1-GPU FP8)"
    serving.kserve.io/deploymentMode: RawDeployment
    opendatahub.io/model-type: generative
    opendatahub.io/genai-use-case: "agentic tool-calling"
    security.opendatahub.io/enable-auth: "false"
spec:
  predictor:
    # Always running — workhorse for RAG, MCP, Guardrails (Steps 06-12)
    minReplicas: 1
    maxReplicas: 1
    # Recreate avoids GPU resource contention during updates
    deploymentStrategy:
      type: Recreate
    nodeSelector:
      node.kubernetes.io/instance-type: g6.4xlarge
    tolerations:
      - key: nvidia.com/gpu
        operator: Equal
        value: "true"
        effect: NoSchedule
    model:
      modelFormat:
        name: vLLM
      runtime: vllm-runtime
      # Pulled via cluster pull secret from registry.redhat.io (no S3/MinIO needed)
      storageUri: oci://registry.redhat.io/rhelai1/modelcar-granite-3-1-8b-instruct-fp8-dynamic:1.5
      args:
        - --served-model-name=granite-8b-agent
        # 16K context REQUIRED: MCP tool schemas (31 tools) + file_search chunks = 12-16K input
        # Do NOT reduce to 8192 — breaks step-10 MCP demo (4 scenes fail)
        - --max-model-len=16384
        # Tuned: 0.92 is the safe maximum before CUDA graph capture OOM (0.95 fails)
        - --gpu-memory-utilization=0.92
        # FP8 KV cache: L4 (Ada Lovelace) has native FP8 support.
        # Doubles KV cache capacity: 74K→155K tokens, max concurrency 4.6x→9.5x at 16K context
        - --kv-cache-dtype=fp8
        - --trust-remote-code
        - --enable-auto-tool-choice
        - --tool-call-parser=granite
        - --chat-template=/opt/app-root/template/tool_chat_template_granite.jinja
      env:
        # RHOAI 3.3 GA: V1 engine required
        - name: VLLM_USE_V1
          value: "1"
        - name: VLLM_NO_HW_METRICS
          value: "1"
        - name: LD_LIBRARY_PATH
          value: /usr/local/nvidia/lib64
      resources:
        requests:
          cpu: "8"
          memory: 32Gi
          nvidia.com/gpu: "1"
        limits:
          cpu: "16"
          memory: 60Gi
          nvidia.com/gpu: "1"

