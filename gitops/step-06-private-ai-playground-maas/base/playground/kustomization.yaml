apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Playground Backend Components
# Provides the LlamaStack backend for GenAI Studio Playground

resources:
  - llamastack.yaml
  # MCP server discovery for Dashboard Playground UI (redhat-ods-applications namespace)
  # Ref: RHOAI 3.3 docs — "Configuring Model Context Protocol servers"
  - mcp-servers-configmap.yaml

