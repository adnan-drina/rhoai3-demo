# ⚠️ DEMO VALUES ONLY - Replace for production use
apiVersion: v1
kind: Secret
metadata:
  name: postgresql-credentials
  namespace: private-ai
  labels:
    app.kubernetes.io/part-of: mcp-integration
    app.kubernetes.io/component: database
  annotations:
    argocd.argoproj.io/sync-wave: "1"
type: Opaque
stringData:
  POSTGRESQL_USER: acmeadmin
  POSTGRESQL_PASSWORD: acme-demo-password
  POSTGRESQL_DATABASE: acme_equipment
