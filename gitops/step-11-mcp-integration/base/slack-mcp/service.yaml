apiVersion: v1
kind: Service
metadata:
  name: slack-mcp
  namespace: private-ai
  labels:
    app: slack-mcp
    app.kubernetes.io/part-of: mcp-integration
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: ClusterIP
  selector:
    app: slack-mcp
  ports:
    - name: sse
      port: 8080
      targetPort: 8080
      protocol: TCP
