apiVersion: v1
kind: Service
metadata:
  name: openshift-mcp
  namespace: private-ai
  labels:
    app: openshift-mcp
    app.kubernetes.io/part-of: mcp-integration
  annotations:
    argocd.argoproj.io/sync-wave: "3"
spec:
  type: ClusterIP
  selector:
    app: openshift-mcp
  ports:
    - name: sse
      port: 8000
      targetPort: 8000
      protocol: TCP
