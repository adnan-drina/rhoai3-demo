apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: step-01-gpu
  namespace: openshift-gitops
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://github.com/adnan-drina/rhoai3-demo.git
    targetRevision: main
    path: gitops/step-01-gpu/base
  destination:
    server: https://kubernetes.default.svc
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
      - ServerSideApply=true
      - RespectIgnoreDifferences=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
  ignoreDifferences:
    # Cluster monitoring config - may have additional fields from monitoring operator
    - group: ""
      kind: ConfigMap
      namespace: openshift-monitoring
      name: cluster-monitoring-config
      jsonPointers:
        - /data
        - /metadata/annotations
    # Namespaces may have additional annotations/labels from OLM
    - group: ""
      kind: Namespace
      jsonPointers:
        - /metadata/annotations
        - /metadata/labels
    # OperatorGroups have status fields managed by OLM
    - group: operators.coreos.com
      kind: OperatorGroup
      jsonPointers:
        - /metadata/annotations
    # Subscriptions have fields managed by OLM
    - group: operators.coreos.com
      kind: Subscription
      jsonPointers:
        - /metadata/annotations
        - /spec/startingCSV
    # ClusterPolicy fields managed by GPU Operator
    - group: nvidia.com
      kind: ClusterPolicy
      jqPathExpressions:
        - .spec
    # NFD instance fields managed by operator
    - group: nfd.openshift.io
      kind: NodeFeatureDiscovery
      jqPathExpressions:
        - .spec
    # KnativeServing fields managed by Serverless operator
    - group: operator.knative.dev
      kind: KnativeServing
      jsonPointers:
        - /metadata/annotations
        - /spec
