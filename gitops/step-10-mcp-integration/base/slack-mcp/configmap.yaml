apiVersion: v1
kind: ConfigMap
metadata:
  name: slack-mcp-config
  namespace: private-ai
  labels:
    app.kubernetes.io/part-of: mcp-integration
  annotations:
    argocd.argoproj.io/sync-wave: "3"
data:
  SLACK_WEBHOOK_URL: ""
  DEFAULT_CHANNEL: "#all-acme-mcp-demo"
  DEFAULT_USERNAME: "ACME LithoOps Agent"
