apiVersion: apps/v1
kind: Deployment
metadata:
  name: slack-mcp
  namespace: private-ai
  labels:
    app: slack-mcp
    app.kubernetes.io/name: slack-mcp
    app.kubernetes.io/part-of: mcp-integration
    app.kubernetes.io/component: mcp-server
  annotations:
    argocd.argoproj.io/sync-wave: "3"
    image.openshift.io/triggers: '[{"from":{"kind":"ImageStreamTag","name":"slack-mcp:latest"},"fieldPath":"spec.template.spec.containers[?(@.name==\"slack-mcp\")].image"}]'
spec:
  replicas: 1
  selector:
    matchLabels:
      app: slack-mcp
  template:
    metadata:
      labels:
        app: slack-mcp
        app.kubernetes.io/part-of: mcp-integration
    spec:
      securityContext:
        runAsNonRoot: true
      containers:
        - name: slack-mcp
          image: image-registry.openshift-image-registry.svc:5000/private-ai/slack-mcp:latest
          ports:
            - containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: slack-mcp-config
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            tcpSocket:
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
