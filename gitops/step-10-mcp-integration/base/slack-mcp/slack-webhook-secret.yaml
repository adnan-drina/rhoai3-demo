# ⚠️ DEMO VALUES — Slack Bot Token required for catalog MCP server
# Create a Slack App at https://api.slack.com/apps with scopes:
#   channels:history, channels:read, chat:write, reactions:write, users:read, users.profile:read
# Install to workspace and copy the Bot User OAuth Token (xoxb-...)
apiVersion: v1
kind: Secret
metadata:
  name: slack-mcp-credentials
  namespace: private-ai
  labels:
    app.kubernetes.io/part-of: mcp-integration
  annotations:
    argocd.argoproj.io/sync-wave: "3"
type: Opaque
stringData:
  SLACK_BOT_TOKEN: "xoxb-PLACEHOLDER-provide-real-token"
  SLACK_TEAM_ID: "T09J4PGJHNK"
