# The RHOAI Dashboard Playground discovers MCP servers from this ConfigMap
# in the redhat-ods-applications namespace.
# Each key is a unique MCP server name (case-sensitive, displayed in the UI).
# Each value is a JSON object with:
#   - url:         SSE endpoint URL (cluster-internal or external)
#   - description: Shown in the Dashboard UI when selecting MCP servers
# Users authorize and enable MCP servers in the Playground UI per session.
# Ref: https://docs.redhat.com/en/documentation/red_hat_openshift_ai_self-managed/3.3/html/experimenting_with_models_in_the_gen_ai_playground/playground-prerequisites_rhoai-user#configuring-model-context-protocol-servers_rhoai-user
apiVersion: v1
kind: ConfigMap
metadata:
  name: gen-ai-aa-mcp-servers
  namespace: redhat-ods-applications
  labels:
    app.kubernetes.io/part-of: private-ai-platform
  annotations:
    argocd.argoproj.io/sync-wave: "3"
  # deploy.sh patches these URLs with cluster-specific route hostnames at deploy time.
data: {}
