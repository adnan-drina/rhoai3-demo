apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Sync-wave ordering: infrastructure (1-2) → platform (3-4) → ingestion (5) → UI (5-10)

resources:
  - postgresql/
  - docling/
  - minio-rag-bucket/
  - dspa/
  - llamastack-rag/
  - ingestion-service/
  - rag-wb/
  - chatbot/
