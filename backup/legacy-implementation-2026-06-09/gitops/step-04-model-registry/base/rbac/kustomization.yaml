apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Model Registry RBAC
# - ai-developer: Can register and deploy models
# - ai-admin: Full access including archive/delete

resources:
  - registry-user-rolebinding.yaml
  - registry-admin-rolebinding.yaml

