apiVersion: v1
kind: Service
metadata:
  name: enterprise-ai-registry-internal
  namespace: rhoai-model-registries
  labels:
    app: enterprise-ai-registry
    component: model-registry
  annotations:
    argocd.argoproj.io/sync-wave: "6"
    description: |
      Internal service for cluster-internal API access (port 8080).
      The primary service (enterprise-ai-registry:8443) remains OAuth-protected.
      This is used by automation jobs for model registration.
spec:
  ports:
    - name: http-api
      port: 8080
      protocol: TCP
      targetPort: 8080  # Raw REST port, bypassing OAuth proxy
  selector:
    app: enterprise-ai-registry
    component: model-registry
  type: ClusterIP

